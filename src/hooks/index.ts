/**
 * HOOKS INDEX
 * ===========
 * Central export for all custom hooks.
 */

export { useDebounce } from './use-debounce';
export {
  useMediaQuery,
  useIsMobile,
  useIsTablet,
  useIsDesktop,
} from './use-media-query';
